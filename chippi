#!/usr/bin/env python3
"""
Chippi CLI launcher — agentic OS for U.S. real estate professionals.

This wrapper should behave like the installed `chippi` command, including
subcommands such as `gateway`, `cron`, and `doctor`. It launches only the
agent framework; the Next.js CRM lives in `crm/` and has its own setup.
"""

if __name__ == "__main__":
    from chippi_cli.main import main
    main()
